{
  "name": "@redline/extension",
  "version": "1.0.0",
  "description": "Chromium MV3 extension to leave real-time comments on any interface and route them to a local AI coding assistant.",
  "license": "SEE LICENSE IN LICENSE.md",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "package": "vite build && cd dist && zip -qr -FS ../redline-extension.zip . && cd ..",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.6.1",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.2",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "@material-symbols/svg-400": "^0.45.2"
  }
}
