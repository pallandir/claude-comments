{
  "name": "@redline/mcp-server",
  "version": "0.1.0",
  "description": "MCP server that ingests UI comments from the Redline extension and exposes them to Claude Code.",
  "license": "SEE LICENSE IN LICENSE.md",
  "type": "module",
  "bin": {
    "redline-mcp": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
